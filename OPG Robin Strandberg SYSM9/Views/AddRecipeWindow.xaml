<Window x:Class="OPG_Robin_Strandberg_SYSM9.Views.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Width="1000"
        Height="1000"
        Background="{StaticResource AppBackground}"
        TextElement.FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto">

    <Grid>
        <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>

                    <TextBlock Text="CookMaster"
                               Foreground="{StaticResource PrimaryTitle}"
                               FontSize="80"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,32" />

                    <!-- Add Recipe Card -->
                    <materialDesign:Card Padding="40,32" UniformCornerRadius="16" HorizontalAlignment="Center">
                        <StackPanel Width="340" Orientation="Vertical">

                            <TextBlock Text="Add Recipe"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,24" />

                            <TextBlock Text="Title" />
                            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                     materialDesign:HintAssist.Hint="Recipe name" />

                            <TextBlock Text="Ingredients (comma separated)" />
                            <TextBox Text="{Binding Ingredients, UpdateSourceTrigger=PropertyChanged}"
                                     materialDesign:HintAssist.Hint="e.g. Flour, Milk, Eggs"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     Height="72" />

                            <TextBlock Text="Category" Margin="0,8,0,2" />
                            <ComboBox ItemsSource="{Binding Categories}"
                                      SelectedItem="{Binding Category, Mode=TwoWay}"
                                      materialDesign:HintAssist.Hint="Select category" />

                            <TextBlock Text="Instructions" />
                            <TextBox Text="{Binding Instructions, UpdateSourceTrigger=PropertyChanged}"
                                     materialDesign:HintAssist.Hint="Step by step instructions"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     Height="90" />

                            <TextBlock Text="Date Created" Margin="0,8,0,2" />
                            <DatePicker SelectedDate="{Binding CreatedDate, Mode=TwoWay}" />

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                                <Button Content="Save Recipe" Command="{Binding SaveCommand}" />
                                <Button Content="Back" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}" />
                            </StackPanel>

                        </StackPanel>
                    </materialDesign:Card>

                    <TextBlock Text="© 2026 Robin Erik Strandberg"
                               FontSize="11"
                               Foreground="#AAA"
                               HorizontalAlignment="Center"
                               Margin="0,24,0,10" />

                </StackPanel>
            </Grid>
        </Viewbox>
    </Grid>
</Window>
